import 'package:flutter/material.dart';

class AdoptPet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Adopt Pet")),
      body: Center(child: Text("Pet List Will Appear Here")),
    );
  }
}
