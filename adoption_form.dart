import 'package:flutter/material.dart';

class AdoptionForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Adoption Form")),
      body: Center(child: Text("Adoption Form Here")),
    );
  }
}
